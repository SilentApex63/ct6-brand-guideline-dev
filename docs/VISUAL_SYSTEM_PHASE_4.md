# Phase 4 â€” Visual System Pages

This update upgrades the static brand guideline pages that define the core CT6 design language:

- `/colors/`
- `/typography/`
- `/tokens/`

## What changed

### Colors

Adds a richer visual color system with grouped palettes, usage notes, RGB/HEX/token values, and practical do/don't rules.

### Typography

Adds font family cards, type-scale previews, usage notes, and tactical typography rules.

### Tokens

Adds token category cards, implementation snippets, and expanded token tables for color, typography, spacing, radius, shadows, motion, breakpoints, and z-index.

## Deployment note

This update does not create or modify `vercel.json`. The app should continue using Vercel's Next.js project settings with the output directory left blank/default.